{
  "name": "robot-assembly-lab-source-checks",
  "version": "2.0.0",
  "private": true,
  "description": "Optional source-level regression checks for the Java Servlet/JSP application.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/server/**/*.test.cjs"
  }
}
